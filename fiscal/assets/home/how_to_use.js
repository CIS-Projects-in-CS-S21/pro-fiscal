/**
 * Function that displays a list of finance terms for the user to look up.
 * @param {Array} - takes in an array of JSON Objects that are essentially FAQs.
 * @returns {Element} - Rendered component an Array containing the list of FAQs and their answers.
 * @throws {InvalidArgumentException} - If expense is not an array, contains no items, null, etc.
 */
function render_guide (faqs) {

}