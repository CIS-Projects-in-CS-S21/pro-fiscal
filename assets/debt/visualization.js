/**
 * Function that given an Array of debt items (expected_completion_time, principal_reduction, interest_paid,
 * and loan_chart) renders the visualization of monthly payments contributed towards overall loan owned.
 * @param {Array} debt_items - Array of Debt Items associated with the user.
 * @throws {InvalidArgumentException} - If debt is not an array, contains no items, null, etc.
 * @throws Will throw an error if null or an empty debt object is inputted.
 */
function render_payment_chart (debt_items) {

}